export interface ErrorMessage {
  required: string;
  email: string;
  mustMatch: string;
  pattern: string;
  busy: string;
  incorrect: string;
  maxlength: string;
  minlength: string;
  badCurrentPassword: string;
  badSamePassword: string;
}